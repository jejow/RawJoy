import{morph}from"@theme/morph";class SectionRenderer{#cache=new Map;#abortControllersBySectionId=new Map;#pendingPromises=new Map;constructor(){window.addEventListener("load",this.#cachePageSections.bind(this))}async renderSection(sectionId,options){const{cache=!Shopify.designMode}=options??{};this.#abortPendingMorph(sectionId);const abortController=new AbortController;this.#abortControllersBySectionId.set(sectionId,abortController);const sectionHTML=await this.getSectionHTML(sectionId,cache);return abortController.signal.aborted||(this.#abortControllersBySectionId.delete(sectionId),morphSection(sectionId,sectionHTML)),sectionHTML}#abortPendingMorph(sectionId){const existingAbortController=this.#abortControllersBySectionId.get(sectionId);existingAbortController&&existingAbortController.abort()}async getSectionHTML(sectionId,useCache=!0,url=new URL(window.location.href)){const sectionUrl=buildSectionRenderingURL(sectionId,url);let pendingPromise=this.#pendingPromises.get(sectionUrl);if(pendingPromise)return pendingPromise;if(useCache){const cachedHTML=this.#cache.get(sectionUrl);if(cachedHTML)return cachedHTML}pendingPromise=fetch(sectionUrl).then(response=>response.text()),this.#pendingPromises.set(sectionUrl,pendingPromise);const sectionHTML=await pendingPromise;return this.#pendingPromises.delete(sectionUrl),this.#cache.set(sectionUrl,sectionHTML),sectionHTML}#cachePageSections(){for(const section of document.querySelectorAll(".shopify-section")){const url=buildSectionRenderingURL(section.id);if(this.#cache.get(url)||containsShadowRoot(section))return;this.#cache.set(url,section.outerHTML)}}}const SECTION_ID_PREFIX="shopify-section-";function buildSectionRenderingURL(sectionId,url=new URL(window.location.href)){return url.searchParams.set("section_id",normalizeSectionId(sectionId)),url.searchParams.sort(),url.toString()}export function buildSectionSelector(sectionId){return`${SECTION_ID_PREFIX}${sectionId}`}export function normalizeSectionId(sectionId){return sectionId.replace(new RegExp(`^${SECTION_ID_PREFIX}`),"")}function containsShadowRoot(element){return!!element.shadowRoot||Array.from(element.children).some(containsShadowRoot)}export async function morphSection(sectionId,html){const fragment=new DOMParser().parseFromString(html,"text/html"),existingElement=document.getElementById(buildSectionSelector(sectionId)),newElement=fragment.getElementById(buildSectionSelector(sectionId));if(!existingElement)throw new Error(`Section ${sectionId} not found`);if(!newElement)throw new Error(`Section ${sectionId} not found in the section rendering response`);if(sectionId.indexOf("cart-drawer")!==-1){const existingInner=existingElement.querySelector(".cart-drawer__inner"),newInner=newElement.querySelector(".cart-drawer__inner");if(existingInner&&newInner){morph(existingInner,newInner);console.log("[SectionRenderer] Morphed cart-drawer__inner in-place.");return}}const existingDialogs=existingElement.querySelectorAll("dialog"),newDialogs=newElement.querySelectorAll("dialog");existingDialogs.forEach((dialog,idx)=>{dialog.open&&newDialogs[idx]&&newDialogs[idx].setAttribute("open","")});console.log("[SectionRenderer] morphSection dialog open states before morph:", Array.from(existingDialogs).map(d => d.open));const modalDialogs=[];existingDialogs.forEach((dialog,idx)=>{dialog.matches&&dialog.matches(":modal")&&modalDialogs.push({id:dialog.id,index:idx})});morph(existingElement,newElement);modalDialogs.forEach(info=>{let dialogElement=null;if(info.id){dialogElement=existingElement.querySelector(`#${info.id}`)}if(!dialogElement){const dialogs=existingElement.querySelectorAll("dialog");dialogElement=dialogs[info.index]}if(dialogElement&&!dialogElement.matches(":modal")){dialogElement.removeAttribute("open");try{dialogElement.showModal();dialogElement.setAttribute("open","");console.log(`[SectionRenderer] Restored modal state for dialog:`,dialogElement.id||info.index)}catch(e){console.error(`[SectionRenderer] Failed to restore modal state:`,e)}}})}export const sectionRenderer=new SectionRenderer;
//# sourceMappingURL=/cdn/shop/t/2/assets/section-renderer.js.map?v=154568302229434357571765250349
